# Add these lines to your .env file to improve worker performance

# Enable task runners (recommended by n8n)
N8N_RUNNERS_ENABLED=true

# Offload manual executions to workers
OFFLOAD_MANUAL_EXECUTIONS_TO_WORKERS=true

# Optional: Increase worker concurrency (default is 10)
# N8N_WORKER_CONCURRENCY=20

# Optional: Adjust queue settings for better performance
# QUEUE_BULL_REDIS_DB=0
# QUEUE_BULL_REDIS_PREFIX=bull
# QUEUE_BULL_REDIS_TIMEOUT=5000
# QUEUE_BULL_REDIS_MAX_RETRIES=3
